Empty on purpose. The robot dog is built procedurally in `js/robotDog.js`,
so there's no external model file to load. Kept as a placeholder in case
you later swap in a licensed external model — see the main README's
"External assets" section.
